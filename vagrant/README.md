Setup Vagrant Dev Environment
-----------------------------

Download and install Vagrant and VirtualBox as per the Vagrant "Getting started" page.

Install the Ansible Vagrant plugin:

    `vagrant gem install vagrant-ansible`

