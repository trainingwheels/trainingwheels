<div id="course-form">
  <form>
    <div id="course-fields">
      <div class="field">Title:       {{view App.LazyTextField valueBinding="view.title" viewName="titleTextField"}}</div>
      <div class="field">Description: {{view App.LazyTextField valueBinding="view.description" viewName="descriptionTextField"}}</div>
      <div class="field">Name:        {{view App.LazyTextField valueBinding="view.name" viewName="nameTextField"}}</div>
      <div class="field">Type:        {{view App.LazyTextField valueBinding="view.type" viewName="typeTextField"}}</div>
      <div class="field">Environment: {{view App.LazyTextField valueBinding="view.environment" viewName="environmentTextField"}}</div>
      <div class="field">Repository:  {{view App.LazyTextField valueBinding="view.repository" viewName="repositoryTextField"}}</div>
      <div class="field">Host:        {{view App.LazyTextField valueBinding="view.host" viewName="hostTextField"}}</div>
    </div>

    <div id="course-tools">
      <button type="submit" value="Save" {{action saveCourse target="view"}}>Save</button>
    </div>
  </form>
</div>
